{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {loadImage = id; loadMovie = id; playOrPauseMovie = id; rot13 = id; }; 
            CLASS = MyObject; 
            LANGUAGE = ObjC; 
            OUTLETS = {aMovieView = id; aTextField = NSTextField; anImageView = id; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}